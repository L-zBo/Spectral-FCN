# =====================================================
# 拉曼光谱微塑料分类项目 - 依赖环境
# Raman Spectrum Microplastic Classification Project
# =====================================================

# -----------------
# 深度学习框架
# -----------------
# PyTorch - 用于构建和训练FCN神经网络模型
torch>=1.9.0

# torchvision - PyTorch的计算机视觉库
torchvision>=0.10.0

# -----------------
# 数据处理库
# -----------------
# NumPy - 用于数值计算和数组操作
numpy>=1.21.0

# Pandas - 用于CSV光谱数据的读取和处理
pandas>=1.3.0

# -----------------
# 机器学习库
# -----------------
# Scikit-learn - 提供传统机器学习算法(KNN, SVM, RF)和评估指标(F1, Recall)
scikit-learn>=1.0.0

# -----------------
# 可视化库
# -----------------
# Matplotlib - 用于绘制对比图表、训练曲线、丰度图
matplotlib>=3.4.0

# Seaborn - 统计数据可视化（可选）
seaborn>=0.11.0

# -----------------
# 模型解释性库
# -----------------
# SHAP - 用于计算 Shapley 值，解释模型决策（可选）
shap>=0.41.0

# =====================================================
# 运行环境说明
# =====================================================
# Python版本: >= 3.8
#
# GPU支持（推荐）:
#   - CUDA >= 11.0
#   - 安装对应版本的 torch (如 torch+cu118)
#
# CPU运行:
#   - 支持纯CPU运行，但训练速度较慢
#
# 安装命令:
#   pip install -r requirements.txt
#
# GPU版本安装（以CUDA 11.8为例）:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# =====================================================
